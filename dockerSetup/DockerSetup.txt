# Download the docker image
sudo docker pull osrf/ros:noetic-desktop

# Create the Docker Container
docker run -it --name ros --net=host --env="DISPLAY=host.docker.internal:0.0" -v /home/arthik/DockerROS/:/DockerROS -v /tmp/.X11-unix:/tmp/.X11-unix osrf/ros:noetic-desktop

# Start the container
docker start -i ros

# ssh into pi
ssh -X pi@192.168.12.1

#
